import React from "react";
import { View, Text, StyleSheet } from "react-native";

const StylePhotoScreen = () => {
	return (
		<View>
			<Text>Account Screen</Text>
		</View>
	);
};

const styles = StyleSheet.create({});

export default StylePhotoScreen;
