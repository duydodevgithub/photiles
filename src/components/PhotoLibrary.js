import React from "react";
import { Text, View, StyleSheet } from "react-native";

const PhotoLibrary = () => {
	return (
		<View>
			<Text>PhotoLigrary</Text>
		</View>
	);
};

const styles = StyleSheet.create({});

export default PhotoLibrary;
